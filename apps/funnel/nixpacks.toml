[phases.setup]
nixPkgs = ["nodejs_18", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile --no-strict-peer-dependencies"]

[phases.build]
cmds = ["pnpm build --filter=@anplexa/funnel"]

[start]
cmd = "pnpm start"
